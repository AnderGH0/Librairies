const images = document.querySelector(".images");
const imgGrid = images.querySelectorAll(".img");

//clicking images
/* for(let i = 0; i < imgGrid.length; i++){
    const mask = imgGrid[i].querySelector("div")
    imgGrid[i].addEventListener("click", ()=>{
        maskAll();
        mask.classList.toggle("mask");
    })
}
const maskAll = () => {
    for(let img of imgGrid){
        img.querySelector("div").classList.add("mask");
    }
}; */
//swiping text
